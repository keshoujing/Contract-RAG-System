# Contract-RAG

项目根目录下有 `memory/` 目录，记录了本项目各模块的技术方案、实验结论和已踩过的坑。

需要了解某个模块的实现思路、或准备开发新功能时，先读 `memory/MEMORY.md` 找到对应的方案文件，再读具体文件。有新的实验结论或设计决策落地时，更新对应的方案文件。

后端对前端暴露的接口契约维护在 `docs/INTERFACE.md`（审批抽取 + Excel 台账同步）。改动这些对外函数/数据结构时，必须同步更新该文档，保持其与代码一致。
